/**
 * @file EntityManager_spec.ts - Entity Manager Spec
 * @author Michael Robertson
 * @version 0.0.1
 */
import { EntityManager } from "../../src/domain/EntityManager";

describe("EntityManager", () => {
  describe("getRepository()", () => {
    it("should return a repository", () => {
      // object under test
      const em: EntityManager = new EntityManager();
      // expect(EntityManager.getRepository("users")).to.be.instanceof(typeof Mock);
    });

  });
});
