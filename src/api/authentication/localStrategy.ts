///**
// * @file localStrategy.ts - passport strategy manager
// * @author Michael Robertson
// * @version 0.0.1
// */
//
//import * as LocalStrategy from "passport-Local";
//import { User } from "../../shared/entities/User";
//
//type IVerifyOptions = { message: string; };
//type VerifyFunction = (username: string,
// password: string,
// done: (error: Error,
// user?: User,
// options?: IVerifyOptions) => void) => void;
//
//class PassportStrategyManager {
//  public authenticate: VerifyFunction = (username: string,
// password: string,
// done: (error: Error, user?: User, options?: IVerifyOptions) => void): => {
// };
//  // public localStrategy: Function = async (): => {
//  // };
//}
//
//let authenticate: VerifyFunction = (): => {};
